token: xxxx
webhookURLs:
- "https://discord.com/api/webhooks/xxxxx/xxxxx"
chatChannelIDs:
- 000000000000000000
playingGameName: サーバー稼働中！（{players}/{max}人）

prefix: "!"
adminRole: ""
updateCheck: true

# Formats
fromDiscordToMinecraftFormat: "§9[Discord§7({server_name})§9] §f<{name}({tag})> "
fromMinecraftToDiscordName: "Minecraft: ({server}) {displayName}"
fromDiscordToDiscordName: "Discord({server_name}): {nickName}({tag})"

hiddenServers:
  - test
dummyServerName: "----"
# LunaChat使用時にのみ有効 (Only when using the LunaChat plugin)
japanizeFormat: "*({server})* **{sender}** __JP__: {japanized}"

# この値は変更しないでください！ (Don't change the value!)
configVersion: 4